# Database Configuration
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/your_database

# Session Configuration (for production, use a strong secret)
SESSION_SECRET=your-secret-key

# For local development with PostgreSQL:
# 1. Create a database called 'todoengine' in PostgreSQL
# 2. Replace the placeholders below with your local PostgreSQL credentials
PGUSER=your_username
PGPASSWORD=your_password
PGHOST=localhost
PGPORT=5432
PGDATABASE=todoengine

# Example DATABASE_URL format:
# DATABASE_URL=postgresql://username:password@localhost:5432/todoengine